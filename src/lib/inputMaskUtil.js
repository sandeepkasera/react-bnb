export function applyDateMask(dateInputElem) {  let value = dateInputElem.value;  const initialCaretPos = dateInputElem.selectionStart;  const initialValueLength = value.length;  value = value.replace(/[^0-9]/g, '');  let formattedValue = value.substring(0, 4);  if (value.length > 4) {    formattedValue += '-' + value.substring(4, 6);  }  if (value.length > 6) {    formattedValue += '-' + value.substring(6, 8);  }  dateInputElem.value = formattedValue;  dateInputElem.selectionStart = initialCaretPos + (formattedValue.length - initialValueLength);  dateInputElem.selectionEnd = initialCaretPos + (formattedValue.length - initialValueLength);  return formattedValue;}